using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Packet to say that a user has disconnected from a server (NOT IMPLEMENTED)
/// </summary>
public class InfoUserDisconnectedFromServer : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
